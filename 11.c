# include<stdio.h>
#include<conio.h>
int main()
{
char ch;
printf("enter anyb character:");
scanf("%c",&c);
if(ch>='a' && ch<='z' || (ch>='A' && ch<='Z'))
{
printf("character is alphabet");
else
{
printf("character is not alphabet");
}
return 0
}
